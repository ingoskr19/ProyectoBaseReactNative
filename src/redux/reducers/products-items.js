import { ADD_TO_CART, REMOVE_FROM_CART } from "../actionTypes/cart-action-types";

const prouctsItems = (state = [], action) => {

    switch(action.type){
        default : 
        return state;
    }

};

export default prouctsItems;
